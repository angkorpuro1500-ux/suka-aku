<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Kamu Suka Aku?</title>
<style>
  body {
    font-family: 'Poppins', sans-serif;
    text-align: center;
    margin: 0;
    height: 100vh;
    overflow: hidden;
    background: linear-gradient(135deg, #ffe6eb, #fff0f5);
  }

  h2 {
    font-size: 30px;
    color: #ff4d6d;
    margin-top: 120px;
  }

  .button {
    padding: 12px 26px;
    margin: 20px;
    font-size: 18px;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    transition: transform 0.2s ease, background-color 0.2s ease;
  }

  .ya {
    background-color: #ff4d6d;
    color: white;
    box-shadow: 0 4px 8px rgba(255, 77, 109, 0.4);
  }

  .ya:hover {
    background-color: #ff1e50;
    transform: scale(1.1);
  }

  .tidak {
    background-color: #b0b0b0;
    color: white;
    position: absolute;
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
  }

  .heart {
    position: fixed;
    top: -10px;
    color: #ff4d6d;
    font-size: 20px;
    animation: fall linear;
  }

  @keyframes fall {
    0% { transform: translateY(0) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
  }

  /* Tampilan popup GIF setelah klik YA */
  .popup {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(255, 255, 255, 0.95);
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    z-index: 10;
    visibility: hidden;
    opacity: 0;
    transition: all 0.5s ease;
  }

  .popup.show {
    visibility: visible;
    opacity: 1;
  }

  .popup img {
    width: 250px;
    border-radius: 15px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.3);
  }

  .popup h3 {
    color: #ff4d6d;
    margin-top: 20px;
    font-size: 24px;
  }

</style>
</head>
<body>
  <h2>🥰 KAMU SUKA SAMA AKU GA? 🥰</h2>
  <button class="button ya" onclick="jawabYa()">YA 💖</button>
  <button class="button tidak" id="tidakBtn">TIDAK 😝</button>

  <!-- Popup berisi GIF -->
  <div class="popup" id="popup">
    <img src="https://media.tenor.com/ZJ8QOZYmpBEAAAAi/love-you.gif">
    <h3>Awww 💕 Aku juga suka kamu!</h3>
  </div>

  <script>
    const tidakBtn = document.getElementById('tidakBtn');
    const popup = document.getElementById('popup');

    // Tombol "TIDAK" kabur saat didekati
    tidakBtn.addEventListener('mouseover', () => {
      const x = Math.random() * (window.innerWidth - tidakBtn.offsetWidth);
      const y = Math.random() * (window.innerHeight - tidakBtn.offsetHeight);
      tidakBtn.style.left = `${x}px`;
      tidakBtn.style.top = `${y}px`;
    });

    // Jika "YA" diklik
    function jawabYa() {
      popup.classList.add('show');
    }

    // Efek hati jatuh
    setInterval(() => {
      const heart = document.createElement('div');
      heart.classList.add('heart');
      heart.innerHTML = '💖';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = Math.random() * 3 + 2 + 's';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 5000);
    }, 400);
  </script>
</body>
</html>
